{
  "$schema": "http://json.schemastore.org/proxies",
  "proxies": {
    "spa": {
      "matchCondition": {
        "route": "/{*restOfPath}"
      },
      "backendUri": "%FUNCTION_HOST%api/wildcard{restOfPath}"
    }
  }
}